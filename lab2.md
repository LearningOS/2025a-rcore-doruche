### 实现的功能
`sys_gettime()`，`sys_trace()`基本上就把涉及到的指针walk一遍页表就行。

前者对是否跨页分类处理一下；后者读写的操作数据都是1字节的，就更简单了。另外，与上一个实验相比，
把用于追踪系统调用次数的`tracer`转移到了TCB里，这就更清晰了。

有关`sys_mmap`，`sys_munmap`，主要的问题是如何判断是否已存在对应映射。这里考虑在
`MapArea`中增加一个`contains()`方法：对于恒等映射暂不实现，对寻常的物理页帧映射，
我们通过BTree里是否存在对应VPN来判断。接着，每当建立新映射（删除映射）时，对`MemorySet`的
所有`MapArea`作遍历，如果存在（不存在）指定的映射，就报错。其余情况正常处理即可。

还有一些零散的改动，这里就不表。

### 问题回答
1. - [63:54]    均为零，预留
   - [53:10]    PPN
   - [9:8]      RSW，预留给软件
   - [7]        D，脏位
   - [6]        A，访问位
   - [5]        G，全局位
   - [4]        U，用户位
   - [3]        X，执行位
   - [2]        W，写位
   - [1]        R，读位
   - [0]        V，有效位
2. - Instruction access fault, Store/AMO access fault, Load/AMO page fault, Instruction page fault, Store/AMO page fault.
   - `scause`被置为对应的异常代码，`stval`被置为发生异常的虚拟地址。
   - 提高响应速度；避免不必要的内存开销。
   - 20MB。
   - 先按一定比例分配部分内存，并在TCB中的某个部分维护映射信息（比如`MemorySet`）。接着页错误时，检验错误地址是否在某个映射区域内，若是则分配物理页帧并建立映射并正常返回。
   - 取一个保留位表示这种情况。
3. - 此前的全局内核页表仅在启动阶段使用。此后调度器做任务切换时，同时切换各自的页表并清空TLB即可。trap handler中将不必再切换页表。
4. 内核空间页面不设置U位即可。
5. 内核访问用户空间时不再需要软件模拟页表查询，大大提高了效率。
6. 双页表：trap时切换页表；单页表：进程在内核态切换时切换页表。

### 荣誉准则
1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：
   
    null

2. 此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容:
    
   - mmap man page

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。