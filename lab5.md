### 实现的功能
没有严格按照指导书给出的思路实现（银行家算法？）。

总体上，对于Mutex和Semaphore，各自实现一个`DeadlockDetector`结构，
存储在PCB中，同时提供一些方便的上层封装，以及对应的核心的方法：`MutexDeadlockDetector::detect_cycle()`、`SemDeadlockDetector::detect_deadlock()`。

Mutex的情况比较简单，具有必然的持有-释放的控制流。利用这种二值性，
直接将`MutexDeadlockDetector`实现为一个等待图，死锁存在的充要条件
即为这是个有环图。具体来说，它的结构如下：

```rust
/// must be limited in each process
pub struct DeadlockDetector {
    /// lock_id -> thread_id
    lock_holders: BTreeMap<usize, usize>,
    /// thread_id -> lock_id
    wait_requests: BTreeMap<usize, usize>,
}
```

Semaphore复杂一些，它是多实例的资源。我们选择实现为一个资源分配图，
接着，采用类似银行家算法的思路检测死锁。它的结构如下：

```rust
pub struct DeadlockDetector {
    // semaphore_id -> available count
    pub avail: BTreeMap<usize, isize>,
    // thread_id -> (semaphore_id -> allocated count ( P - V ))
    pub alloc: BTreeMap<usize, BTreeMap<usize, isize>>,
    // thread_id -> requested semaphore_id
    pub req: BTreeMap<usize, usize>,
}
```

二者都需要在具体的系统调用接口动态维护自身的状态。此后，死锁检测的关键在于在获取资源处的系统调用处，首先调用死锁检测方法，如果结果为真，则返回错误码`-0XDEAD`，否则继续执行获取资源的操作。

### 问题回答
1. - 需要回收的资源有各个线程的用户栈，TrapFrame等，接着是整个进程的地址空间，同步原语对象，文件描述符表等。
   - 主线程外，其余线程可能在调度队列中，也有可能在Timer的阻塞队列中，它们需要被移除来回收；还有的可能由于阻塞在同步原语被从调度队列中转移到了同步原语的等待队列中，它们不必显式地移除，因为同步原语对象会被回收掉，等待队列自然也就不存在了。
2. 两个实现中，`unlock()`中都应使用`wakeup_task()`而非`add_task()`，因为后者不会做任务状态的转变，
   而此前的`lock()`方法会将阻塞的线程状态改为`TaskState::Blocked`。
   
   实现一是不够公平的。换言之，它并非“先到先得”的。设想被唤醒的线程被放进调度队列尾部，
   然而如果排在它之前的线程也尝试获取锁，且在自身下一次调度之前都不释放呢？这时，
   刚刚被唤醒的线程醒来就会发现锁依然被占用，只能继续沉睡。原实现的`unlock()`中，如果发现
   调度队列中仍存在需要唤醒的任务，就不会将`locked`设置为`false`，才能保证公平性。
   这个实现比原实现多了个循环，是为了保证逻辑的正确性。

   实现二就是原实现。优点上面已经提到过了。

### 附
太蠢了。

因为只给了春季的训练营的指导书，加上我发现其中有些代码和本次的不一致
（比如无栈协程的代码没有提供），所以我安心地想着：
啊，一定是指导书没来得及更新之类的事情没错吧。
那么，我应该从测例中判断需要实现的内容是什么。指导书中提到除了死锁检测以外，
还需要实现`sys_gettime()`，但是我仅仅发现了有关死锁检测的需求，
于是我就只实现了死锁检测。

因为这份大意，发生了很可怕的事情。核心算法早早就写好了，不幸的是
测试始终不能通过。具体表现是，两个测例的主线程都卡在`sleep()`后不动了。

喂，这看起来也太像是死锁检测不成功所以没能避开死锁导致的结果了吧！
所以我反复调试这部分代码，包括但不限于调换一些逻辑的顺序，
打印各种 debug 信息，甚至推倒重来。然而很长时间过去！
......一切努力都没有奏效的样子。

一小时前，我发现了问题所在：结果原来是`sleep()`调用的并非阻塞的定时器版本，
而是一个简简单单的轮询版本，代码不断调用`sys_gettime()`来检查时间是否到了。
而我并没有实现`sys_gettime()`，所以轮询的结果永远是否，导致主线程永远不会醒来。

然后我实现了`sys_gettime()`，测试立即通过了。

非常抱歉的经历。（可恶

### 荣誉准则
1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：
   
    null

2. 此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容:
    
    - opencamp 群友聊天记录

    - gemini 查询相关算法

    - ostep semaphore 相关内容

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。